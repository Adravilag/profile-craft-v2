{
  "buildCommand": "cd app/front-end && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "app/front-end/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
