{
  "buildCommand": "cd app/front-end && pnpm install && pnpm run build",
  "outputDirectory": "app/front-end/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
